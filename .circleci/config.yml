version: 2.1

jobs:
  build-and-test:
    docker:
      - image: mcr.microsoft.com/dotnet/sdk:9.0
      - image: postgres:16-alpine
        environment:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: notifications_test
    working_directory: ~/repo
    environment:
      ConnectionStrings__DefaultConnection: "Host=localhost;Port=5432;Database=notifications_test;Username=postgres;Password=postgres"
    steps:
      - checkout

      - restore_cache:
          keys:
            - dotnet-packages-{{ checksum "NotificationManagement.sln" }}

      - run:
          name: Restore dependencies
          command: dotnet restore NotificationManagement.sln

      - save_cache:
          paths:
            - ~/.nuget/packages
          key: dotnet-packages-{{ checksum "NotificationManagement.sln" }}

      - run:
          name: Install PostgreSQL client
          command: apt-get update && apt-get install -y postgresql-client

      - run:
          name: Wait for PostgreSQL to be ready
          command: |
            until pg_isready -h localhost -p 5432 -U postgres; do
              echo "Waiting for PostgreSQL..."
              sleep 1
            done
            echo "PostgreSQL is ready!"

      - run:
          name: Build
          command: dotnet build NotificationManagement.sln --configuration Release --no-restore

      - run:
          name: Apply EF Core migrations
          command: dotnet ef database update --project NotificationManagement.sln

      - run:
          name: Run tests
          command: dotnet test NotificationManagement.sln --configuration Release --no-build --logger "trx;LogFileName=test-results.trx" --results-directory ./test-results

      - store_test_results:
          path: test-results

workflows:
  build-and-test-workflow:
    jobs:
      - build-and-test